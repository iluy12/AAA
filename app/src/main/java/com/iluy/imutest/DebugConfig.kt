package com.iluy.imutest

/**
 * ⚠️ מצב-דיבאג ל-v1 בלבד. כשדגל זה true, כל טריגר מציג תג "הופעל ע"י:
 * [מקור מדויק]" על המסך (סעיף 12 במסמך המסירה) — כדי לזהות חיישן שמרעיש
 * בבדיקה עצמית.
 *
 * חובה להפוך ל-false לפני כל גרסה שיוצאת למשתמשים אחרים חוץ ממך.
 */
object DebugConfig {
    const val DEBUG_TAG_ENABLED = true

    /** קו-בדיקה בלבד ל-RISK B — לא המרכזייה האמיתית. עדכן למספר שלך. */
    const val TEST_LINE_PHONE_NUMBER = "0552970003"

    /** חלון-בטיחות חיצוני לזיהוי צרור הקשות (בפועל כמעט לעולם לא נחצה —
     * TAP_MAX_INTERVAL_MS עושה את העבודה האמיתית של תיחום-הצרור). */
    const val TAP_WINDOW_MS = 5_000L

    /**
     * כמה דפיקות נדרשות כדי להיחשב טריגר. הורד מ-6 ל-4 — קצב דפיקה-על-
     * דלת טבעי (ההוראה החדשה בשאלון) הוא קצר, 3-4 דפיקות, לא 6-8.
     */
    const val TAP_COUNT_THRESHOLD = 4

    /**
     * עוצמת accelerometer (m/s²) שמעליה קפיצה נחשבת "הקשה" מועמדת.
     */
    const val TAP_MAGNITUDE_THRESHOLD = 22.0

    /**
     * "jerk" — קפיצה מינימלית בעוצמה בין שני מדגמים רצופים. הקשה אמיתית
     * קופצת בבת אחת; תנועת יד רגילה משתנה בהדרגה.
     */
    const val TAP_MIN_DELTA = 12.0

    /** זמן מינימלי בין שתי קפיצות שנספרות (מונע ספירה כפולה מרעד-הקשה). */
    const val TAP_REFRACTORY_MS = 120L

    /**
     * מרווח מקסימלי בין דפיקה לדפיקה כדי להיחשב חלק מאותו "צרור-דפיקה"
     * (בהשראת קצב-דפיקה-על-דלת טבעי). אם עובר יותר מזה — לא נזרק הכל,
     * אלא מתחילים "צרור" חדש מהדפיקה הזו, עם דוגמה חדשה.
     */
    const val TAP_MAX_INTERVAL_MS = 550L

    /**
     * בדיקת-סדירות: סטיית-התקן של המרווחים בין הדפיקות בצרור. הודק מ-220
     * ל-150 — עכשיו יש גם TAP_MAX_INTERVAL_MS וגם הוראה מפורשת למשתמש
     * ("דפוק כמו על דלת"), אז אפשר לדרוש עקביות גבוהה יותר.
     */
    const val TAP_RHYTHM_MAX_STDDEV_MS = 150.0

    /**
     * "פולס קצר וחד": כמה מדגמים רצופים מותר שיישארו מעל סף-העוצמה לפני
     * שדפיקה נחשבת "רעד ממושך" ולא הקשה חדה, ונדחית. הקשה אמיתית = פולס
     * קצר; רעש-רקע (שולחן, דלת אחרת) נוטה להימשך יותר.
     */
    const val TAP_MAX_CONSECUTIVE_ABOVE_THRESHOLD_SAMPLES = 4

    /**
     * דמיון-לדפיקה-ראשונה: הדפיקה הראשונה בכל צרור הופכת ל"דוגמה" —
     * עוצמה ומשך-פולס משלה. כל דפיקה נוספת בצרור חייבת להיות דומה לה,
     * לא רק לעבור סף גלובלי. זה ההימור החדש — תנועה מקרית כמעט אף פעם
     * לא מייצרת כמה זעזועים עקביים זה עם זה; אדם שדופק בכוונה כן.
     */
    const val TAP_SIMILARITY_MAGNITUDE_TOLERANCE = 0.35 // ±35% מעוצמת הדפיקה הראשונה
    const val TAP_SIMILARITY_PULSE_TOLERANCE_SAMPLES = 2 // ±2 מדגמים ממשך-הפולס הראשון

    /** תנועה רציפה מעל זמן זה משתיקה זיהוי הקשות (סעיף 5: 2+ דקות). */
    const val SUSTAINED_MOTION_SUPPRESS_MS = 120_000L

    /**
     * Worn-gating: אם חיישן off-body/דופק זמין, מתעלמים מדפיקות כשהשעון
     * לא על פרק היד. אם אין חיישן זמין בכלל (או הרשאה) — נופל בחזרה
     * ל"תמיד נחשב לבוש" (לא חוסם), ומתעד את זה בלוג פעם אחת כדי שיהיה
     * ברור שהגנה זו לא פעילה בפועל.
     */
    const val WORN_GATING_ENABLED = true

    /** משך "היכון" אחרי הקשה ראשונה בשעה. */
    const val STANDBY_DURATION_MS = 60 * 60 * 1000L

    /** קירור קצר אחרי "הכל טוב" — לא לפתוח שוב מיד באותה תנועה. */
    const val COOLDOWN_MS = 60_000L

    /** מצב-רוח: כפיית Active למשך 30 דק' (שמור לעתיד — v1 עדיין לא תלוי בזה). */
    const val MOOD_FORCE_ACTIVE_MS = 30 * 60 * 1000L
}
