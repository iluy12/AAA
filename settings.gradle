pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "IluyImuTest"
include ':app'
include ':rescue'
// כלי בדיקת-חומרה עצמאי, לשליחה לספקים לפני קנייה. ראו probe/build.gradle.
include ':probe'
